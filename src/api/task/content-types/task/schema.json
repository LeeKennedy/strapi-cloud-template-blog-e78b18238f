{
  "kind": "collectionType",
  "collectionName": "tasks",
  "info": {
    "singularName": "task",
    "pluralName": "tasks",
    "displayName": "Task",
    "description": "Task content type synced from Stardog ontology"
  },
  "options": {
    "draftAndPublish": true
  },
  "pluginOptions": {},
  "attributes": {
    "title": {
      "type": "string",
      "required": true
    },
    "description": {
      "type": "text"
    },
    "slug": {
      "type": "string"
    },
    "purpose": {
      "type": "text",
      "description": "purpose"
    },
    "inputs": {
      "type": "richtext",
      "description": "inputs"
    },
    "elements": {
      "type": "blocks",
      "description": "elements"
    },
    "guidelinesAndTools": {
      "type": "richtext",
      "description": "guidelines and tools"
    },
    "stakeholders": {
      "type": "richtext",
      "description": "stakeholders"
    },
    "outputs": {
      "type": "richtext",
      "description": "outputs"
    },
    "inputOutputDiagram": {
      "type": "media",
      "multiple": false,
      "required": false,
      "allowedTypes": [
        "images"
      ],
      "description": "input output diagram"
    },
    "outcome": {
      "type": "richtext",
      "description": "outcome"
    },
    "usesTechnique": {
      "type": "relation",
      "relation": "manyToMany",
      "target": "api::technique.technique",
      "description": "Relates a Task to the Technique(s) used to perform it.",
      "inversedBy": "usedByTask"
    }
  }
}